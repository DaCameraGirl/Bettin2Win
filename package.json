{
  "name": "bettin2win",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time multi-sport odds dashboard with AI explanations (football, baseball, NASCAR, horse racing, greyhounds).",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.2"
  }
}
