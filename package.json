{
  "name": "bettin2win",
  "version": "0.1.0",
  "private": true,
  "description": "Live odds, best-price tracking, and plain-English betting context for multi-sport dashboards.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "1.49.1",
    "turbo": "^2.1.3",
    "typescript": "^5.6.2"
  }
}
